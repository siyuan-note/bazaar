# 流程层待办（第 2 层之后 → 第 3 层）

本文档列出：Stage / PR Check 已接入 `check.Check`、PR 评论已改为 Issue 格式之后，**尚未完成**、属于流程层第 3 层及周边的工作。能力层规则仍在 `check/`。

相关设计见 [流程设计.md](./流程设计.md)。

## 已完成（第 2 层）

- Stage：解压后调用 `check.Check(ModeStage)`，传入 `OccupiedNames` / `OldName` / `OldVersion` / `AllowThemeJS`
- PR Check：下载 `package.zip` 后调用 `check.Check(ModePR)`，评论按 `01 [rule]` + 中英 Message 渲染
- 共享：`util.LoadOccupiedNames`、`util.DownloadAndUnzipPackageZip`

## 待做：PR 流程规则

| 项 | 说明 |
| --- | --- |
| 一次只能添加/更改一个包 | 添加或更换维护者时数量限制为 1（移除不限）；违反时在评论中说明并标记失败 |
| 自动改 PR 标题 | 仅涉及一个仓库时，标题改为约定格式（类型 + owner/repo） |
| 自动打类型标签 | 如 `plugin` / `theme` / `widget` / `template` / `icon` |
| 换维护者说明链接 | 检测到换维护者时，回复流程说明文档链接 |

## 待做：Stage 失败可追踪

| 项 | 说明 |
| --- | --- |
| 失败汇总出口 | 当前仅日志 + 沿用旧 stage 条目；需汇总到固定 GitHub Issue（或等价可追踪出口），便于维护者与作者看见 |
| Issue 正文格式 | 可复用 PR 评论的 Issue 渲染约定（Rule + MessageZh/MessageEn） |

## 待做：审核状态机自动化

| 项 | 说明 |
| --- | --- |
| CI 标签 | 根据检查结果自动打/换 `ci-failed` / `ci-passed` |
| 后续状态 | `ai-reviewed` / `waiting-author` / `ready-for-manual` / `approved` 的打标与迁移（见流程设计） |

## 后续增强（不必阻塞第 3 层）

- Latest Release 轮询自动复检（`waiting-author` 时指纹比对）
- AI / 规范初审编排
- 插件源码静态分析（独立组件）
- stage JSON 精简、Index 与 Stage 衔接优化

## 建议落地顺序

1. PR：一次一包 + 标题/类型标签（改动面小、立刻减少脏 PR）
2. Stage：失败汇总到固定 Issue
3. 换维护者说明评论
4. CI 状态标签自动化
5. 再做 Release 轮询与 AI 初审
