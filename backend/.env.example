# 文档元数据根目录（默认 backend/data）；仅作兼容/健康检查等，上传文件默认不直接写这里
# DOCUMENT_DATA_DIR=G:\data\siyuan-paperless-docs

# 实际上传文件落盘目录（VuePress public、静态站点根等）；未设置时默认为 {DOCUMENT_DATA_DIR}/files
# PUBLIC_DIR=/www/wwwroot/vuepress-theme-vdoing/docs/.vuepress/public

# 可选：设置后请求须带 Authorization: Token <同一值>，与插件里填的 Token 一致
# API_KEY=your_secret_token
# 兼容旧字段（不建议新项目继续使用）
# PAPERLESS_TOKEN=your_secret_token

# Flask 监听（插件填局域网 IP 时不要只用 127.0.0.1，应监听 0.0.0.0 或省略此项）
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5000
